<% if params[:controller] == 'my' %>
  <%= form_for @project,
              :url => {:controller => 'projects', :action => 'modules', :id => @project.id },
              :html => {:id => 'modules-form',
              :class => "tab_view",
              :method => :post} do |f| %>
    <%= render :partial => 'projects/settings/modules_content' %>
  <% end %>
<% else %>
  <%= render :partial => 'projects/settings/modules_content' %>
<% end %>
