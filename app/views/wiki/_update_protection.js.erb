$('.icon-unlock-alt, .icon-lock').replaceWith('<%= @lock ? escape_javascript(link_to(l(:button_unlock), {:action => 'protect', :id => @page.title, :protected => 0}, :remote => true, :class => 'icon icon-lock', :method => :post)) : escape_javascript(link_to(l(:button_lock), {:action => 'protect', :id => @page.title, :protected => 1}, :remote => true, :class => 'icon icon-unlock-alt', :method => :post))%>');
