<%= form_tag({:controller => 'group_invitations', :action => 'create'}, :remote => true) do %>
  <%= hidden_field_tag 'receiver_id'%>
  <%= hidden_field_tag("#{group}_id", group_id )%>
  <%= submit_tag(l(:group_invitation_send_project_invitation), :confirm => self.l(:text_message_archive_confirmation)) %>
<%end%>